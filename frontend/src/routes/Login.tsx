/**
 * Login — stub until P6 (fastapi-users + JWT).
 */
export default function Login() {
  return (
    <div className="stub-page">
      <div className="stub-card">
        <span className="stub-badge">Coming in P6</span>
        <h1 className="stub-title">Sign in</h1>
        <p className="stub-desc">
          Multi-user authentication is on the roadmap. For now the app runs
          without an account — your analyses aren't saved between sessions yet.
        </p>
      </div>
    </div>
  );
}
